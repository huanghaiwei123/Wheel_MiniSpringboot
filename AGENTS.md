# 项目学习规则(重要,每次都必须遵守)

用户正在从零手写一个 web 框架,处于学习阶段。

## 学习偏好

1. 当用户说要"学习某个项目/代码"时,**默认由用户自己写代码**,不要直接替用户写。
2. 除非先用 question 工具弹窗询问用户"要自己写还是我帮你写",得到明确答复后再动手。
3. 讲解概念、给思路、给代码片段做参考是允许的;但完整的实现代码,先问再写。
4. 用户明确说"帮我写/你来写/直接改"时,才可以直接写。

## 当前项目背景

- 手写 web 框架:Netty 服务器 + IOC 容器 + MVC(路由/参数解析)+ 未来 AOP/RPC/ORM
- 包结构:`web.server`(Netty)、`web.ioc`(容器+注解)、`web.mvc`(路由/分发/参数解析)、`web.core`(ClassScanner 等工具)
- 已实现:精确路由、动态路由 `/user/{id}`、`@RequestParam`、`@PathVariable`、404/500 区分、JSON 响应
- 待用户自己写:`@RequestBody`(POST + JSON body)